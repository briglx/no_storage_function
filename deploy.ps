#!/bin/bash

RESOURCE_GROUP=development
LOCATION=westus

# az group create --name $resourceGroupName --location "$location"
az deployment group create --name "create_function_app" --resource-group $RESOURCE_GROUP --template-file "template.json" --parameters @parameters.json